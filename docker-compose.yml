name: mavenwebapp
services:
  mavenwebapp:
    image: awssandy/maven-web-app:${IMAGE_TAG}
    ports:
    - 9090:8080
    networks:
    - mavenwebappbridge
    restart: always
networks:
  mavenwebappbridge:
    driver: bridge
